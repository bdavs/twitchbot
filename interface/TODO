(/)add fields to all files (classroom, time, etc.)to reading, writing, interface, etc.
    add class length (or end time)
(X)make each file into functions
(/)create interface for entering new events
	(/)allow users to edit/update existing events
	(X)dynamically add as many events as needed
(/)make Current event more prominent on window
(X)make grid dynamically display all future events
()create database in back end
()check for identical entries
()check for overlapping entries
(/)check for missing data
(/)add date from calendar
()update based on current time
(/)add refresh to display

